version: '3.9'

services:
  student_service:
    container_name: student_service
    build:
      context: user/
      dockerfile: Dockerfile
 
    ports:
      - "5050:5050"
    networks:
      - infrastructure_application
    restart: unless-stopped

networks:
  infrastructure_application:
    external: true

    