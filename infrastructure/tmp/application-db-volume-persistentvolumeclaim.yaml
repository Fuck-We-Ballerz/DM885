apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: application-db-volume
  name: application-db-volume
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
